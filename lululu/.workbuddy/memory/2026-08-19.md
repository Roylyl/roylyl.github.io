# 2026-08-19

## 加密鹿（lululu）逐字输出改造
- 需求：不要显得只有前端；加密/解密逐字输出；短文本稍慢、长文本稍快。
- 实现：script.js 增加「加密引擎」面板（密钥派生→替换表→逐字编织，含进度条与日志）+ typewrite 基于时间的逐字流式输出；pickSpeed 按输出长度自适应间隔（42ms→6ms，总长兜底 ≤3.5s）；busy 状态禁用按钮防重复。
- index.html 增加 subtitle 与 engine 面板；style.css 增加引擎面板/脉冲点/进度条/输出呼吸态样式。
- 加解密核心逻辑（FNV-1a + xorshift32 替换表 + 鹿群符号）未改动，node 往返测试通过。
